<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New sensor</h1>

  <%= turbo_frame_tag @sensor do %>
    <%= render "form", sensor: @sensor %>
  <% end %>

  <%= link_to "Back to sensors", sensors_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
